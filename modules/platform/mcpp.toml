[package]
name        = "platform"
version     = "0.1.0"
description = "mbun.platform — cross-platform capability, path, error, and syscall seams"
license     = "MIT"
standard    = "c++26"



[indices]
mbun = { path = "../../mcpp" }

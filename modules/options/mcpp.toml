[package]
name        = "options"
version     = "0.1.0"
description = "mbun.options — typed CLI/runtime/install option models and validation seams"
license     = "MIT"
standard    = "c++26"



[indices]
mbun = { path = "../../mcpp" }
